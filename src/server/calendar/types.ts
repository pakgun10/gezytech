/**
 * Calendar provider types — re-exports from the SDK. Single source of truth in
 * `packages/sdk/src/index.ts`.
 */
export type {
  CalendarCapabilities,
  CalendarRef,
  EventAttendee,
  CalendarEvent,
  EventListOptions,
  EventListResult,
  CreateEventParams,
  UpdateEventParams,
  CalendarProvider,
} from '@gezy/sdk'
