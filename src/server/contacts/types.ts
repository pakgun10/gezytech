/**
 * Contacts provider types — re-exports from the SDK. Single source of truth in
 * `packages/sdk/src/index.ts`.
 */
export type {
  ContactsCapabilities,
  ContactPhone,
  ContactEmailAddress,
  Contact,
  ContactSearchQuery,
  ContactListOptions,
  ContactListResult,
  ContactsProvider,
} from '@gezy/sdk'
