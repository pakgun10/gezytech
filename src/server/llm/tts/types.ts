/**
 * TTS provider types — re-exports from the SDK. Single source of truth
 * in `packages/sdk/src/index.ts`.
 */
export type {
  Voice,
  SpeakRequest,
  SpeakResult,
  TTSCapabilities,
  TTSProvider,
} from '@gezy/sdk'
