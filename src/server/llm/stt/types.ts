/**
 * STT provider types — re-exports from the SDK. Single source of truth
 * in `packages/sdk/src/index.ts`.
 */
export type {
  TranscriptionModel,
  TranscribeRequest,
  TranscribeResult,
  STTCapabilities,
  STTProvider,
} from '@gezy/sdk'
