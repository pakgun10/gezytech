/**
 * Search provider types — re-exports from the SDK. Single source of truth
 * in `packages/sdk/src/index.ts`.
 */
export type {
  SearchCapabilities,
  SearchRequest,
  SearchResultEntry,
  SearchAnswer,
  SearchResult,
  SearchProvider,
} from '@gezy/sdk'
