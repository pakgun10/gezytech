/**
 * Image provider types — re-exports from the SDK. Single source of truth
 * in `packages/sdk/src/index.ts`.
 */
export type {
  ImageModel,
  ImageRequest,
  ImageResult,
  ImageProvider,
} from '@gezy/sdk'
