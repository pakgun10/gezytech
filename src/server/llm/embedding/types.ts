/**
 * Embedding provider types — re-exports from the SDK. Single source of
 * truth in `packages/sdk/src/index.ts`.
 */
export type {
  EmbeddingModel,
  EmbedRequest,
  EmbedResult,
  EmbeddingProvider,
} from '@gezy/sdk'
